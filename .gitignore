# This tree is a curated, synced artifact (see tools/sync_to_dataset_repo.sh in the
# dev repo) — it should already be clean. This is just a safety net for anyone
# working directly inside it (e.g. re-running a benchmark script locally).
__pycache__/
*.py[cod]
.vscode/
.claude/

# Local-only push helper — not meant to be part of the published artifact.
/do_git.sh
